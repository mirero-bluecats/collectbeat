input {
   http {
      port => 7701
      remote_host_target_field => "remotehost"
   }
}

output {
   kafka {
      bootstrap_servers => "${BLUECATS_KAFKA_HOST}"
      codec => json
      topic_id => "%{[tag]}"
   }
}